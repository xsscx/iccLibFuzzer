diff --git a/dev/fd/63 b/dev/fd/62
--- a/dev/fd/63
+++ b/dev/fd/62
@@ -590,7 +590,7 @@ bool CIccProfile::DeleteTag(icSignature sig)
     CIccTag *pTag = i->pTag;
     m_Tags->erase(i);
 
-    if (!GetTag(pTag)) {
+    if (pTag && !GetTag(pTag)) {
       DetachTag(pTag);
       delete pTag;
     }
@@ -1478,24 +1478,16 @@ icValidateStatus CIccProfile::CheckHeader(std::string &sReport) const
   if (m_Header.deviceClass==icSigMaterialIdentificationClass ||
       m_Header.deviceClass==icSigMaterialLinkClass ||
       m_Header.deviceClass==icSigMaterialVisualizationClass) {
-    icUInt32Number mcs_val;
-    memcpy(&mcs_val, &m_Header.mcs, sizeof(icUInt32Number));
-    if (icGetColorSpaceType((icColorSpaceSignature)mcs_val)!=icSigSrcMCSChannelData) {
+    if (icGetColorSpaceType(m_Header.mcs)!=icSigSrcMCSChannelData) {
       sReport += icMsgValidateCriticalError;
       sReport += " - Invalid MCS designator\n";
       rv = icMaxStatus(rv, icValidateCriticalError);
     }
   }
-  else {
-    icUInt32Number mcs_val, dev_class_val;
-    memcpy(&mcs_val, &m_Header.mcs, sizeof(icUInt32Number));
-    memcpy(&dev_class_val, &m_Header.deviceClass, sizeof(icUInt32Number));
-    
-    if (mcs_val != icSigNoMCSData && dev_class_val != icSigInputClass) {
-      sReport += icMsgValidateNonCompliant;
-      sReport += " - Invalid MCS designator for device class\n";
-      rv = icMaxStatus(rv, icValidateNonCompliant);
-    }
+  else if (m_Header.mcs != icSigNoMCSData && m_Header.deviceClass != icSigInputClass) {
+    sReport += icMsgValidateNonCompliant;
+    sReport += " - Invalid MCS designator for device class\n";
+    rv = icMaxStatus(rv, icValidateNonCompliant);
   }
  
   if (m_Header.colorSpace!=icSigNoColorData ||
@@ -1598,8 +1590,7 @@ icValidateStatus CIccProfile::CheckHeader(std::string &sReport) const
         case icSigBiSpectralReflectanceData:
         case icSigSparseMatrixReflectanceData:
           if (icGetColorSpaceType(m_Header.spectralPCS)==icSigBiSpectralReflectanceData) {
-            icUInt32Number expectedChannels = (icUInt32Number)m_Header.biSpectralRange.steps * (icUInt32Number)m_Header.spectralRange.steps;
-            if (icNumColorSpaceChannels(m_Header.spectralPCS)!=expectedChannels) {
+            if (icNumColorSpaceChannels(m_Header.spectralPCS)!=m_Header.biSpectralRange.steps * m_Header.spectralRange.steps) {
               sReport += icMsgValidateCriticalError;
               sReport += "Number of channels defined for spectral PCS do not match spectral range definitions.\n";
               rv = icMaxStatus(rv, icValidateCriticalError);
@@ -1976,23 +1967,28 @@ icValidateStatus CIccProfile::CheckTagTypes(std::string &sReport) const
   const size_t bufSize = 128;
   icChar buf[bufSize];
   CIccInfo Info;
-
-  icTagSignature tagsig;
-  icTagTypeSignature typesig;
-  icStructSignature structSig;
-  icArraySignature arraySig;
+  
+  // if the tag list pointer is NULL, this would be a serious problem
+// NOTE - ccox - a better solution would be to get rid of the pointer and put the container directly in the class, ditto m_TagVals
+// but that will require changes to several files, and more testing
+  if (!m_Tags)
+    return icValidateCriticalError;
+  
   TagEntryList::const_iterator i;
-  for (i=m_Tags->begin(); i!=m_Tags->end(); i++) {
-    if (!i->pTag) {
-      sReport += icMsgValidateCriticalError;
-      sReport += " - NULL tag pointer encountered\n";
-      rv = icMaxStatus(rv, icValidateCriticalError);
-      continue;
+  for (i = m_Tags->begin(); i != m_Tags->end(); ++i) {
+    icTagSignature tagsig = i->TagInfo.sig;
+    
+    icTagTypeSignature typesig = icSigUnknownType;
+    icStructSignature structSig = icSigUnknownStruct;
+    icArraySignature arraySig = icSigUnknownArray;
+    
+    // missing the internal tag data would cause a problem, issue #322
+    if (i->pTag) {
+      typesig = i->pTag->GetType();
+      structSig = i->pTag->GetTagStructType();
+      arraySig = i->pTag->GetTagArrayType();
     }
-    tagsig = i->TagInfo.sig;
-    typesig = i->pTag->GetType();
-    structSig = i->pTag->GetTagStructType();
-    arraySig = i->pTag->GetTagArrayType();
+    
     snprintf(buf, bufSize, "%s", Info.GetSigName(tagsig));
     if (!IsTypeValid(tagsig, typesig, structSig, arraySig)) {
       sReport += icMsgValidateNonCompliant;
@@ -2498,7 +2494,7 @@ bool CIccProfile::IsTypeValid(icTagSignature tagSig, icTagTypeSignature typeSig,
  */
 icValidateStatus CIccProfile::CheckRequiredTags(std::string &sReport, const CIccProfile *pParentProfile) const
 {
-  if (m_Tags->size() <= 0) {
+  if (!m_Tags || m_Tags->size() <= 0) {
     sReport += icMsgValidateCriticalError;
     sReport += "No tags present.\n";
     return icValidateCriticalError;
@@ -2975,13 +2971,8 @@ icValidateStatus CIccProfile::Validate(std::string &sReport, std::string sSigPat
   rv = icMaxStatus(rv, CheckTagTypes(sReport));
   TagEntryList::iterator i;
   for (i=m_Tags->begin(); i!=m_Tags->end(); i++) {
-    if (!i->pTag) {
-      sReport += icMsgValidateCriticalError;
-      sReport += " - NULL tag pointer in validation\n";
-      rv = icMaxStatus(rv, icValidateCriticalError);
-      continue;
-    }
-    rv = icMaxStatus(rv, i->pTag->Validate(sSigPath + icGetSigPath(i->TagInfo.sig), sReport, this));
+    if (i->pTag)        // should we give an error if pTag is NULL/Not loaded?
+      rv = icMaxStatus(rv, i->pTag->Validate(sSigPath + icGetSigPath(i->TagInfo.sig), sReport, this));
   }
 
   return rv;
